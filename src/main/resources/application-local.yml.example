# 本地敏感配置（复制为 application-local.yml 后填写，勿提交）
spring:
  datasource:
    url: jdbc:mysql://your-host:3306/notify_hub?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: your_user
    password: your_password
